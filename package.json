{
  "name": "chrome-boxnotes",
  "version": "1.0.0",
  "main": "src/content-script.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "format": "prettier --write **/*.{ts,js}",
    "compile": "tsc -p tsconfig.build.json --noEmit && esbuild src/content-script.ts --bundle --outfile=dist/content-script.js --platform=browser --target=es2020",
    "build": "run-s compile lint format"
  },
  "author": "manhole",
  "license": "MIT",
  "private": true,
  "description": "",
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.28.0",
    "eslint": "^9.17.0",
    "jsdom": "^29.0.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "^4.1.3"
  }
}
